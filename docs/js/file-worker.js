// Compatibility path for Safari versions with OPFS synchronous access only.
self.onmessage=async({data})=>{let access;try{access=await data.handle.createSyncAccessHandle();const bytes=new Uint8Array(await data.file.arrayBuffer());access.truncate(0);let offset=0;while(offset<bytes.length){const count=access.write(bytes.subarray(offset),{at:offset});if(!count)throw new Error('Datei konnte nicht vollständig geschrieben werden.');offset+=count;}access.flush();access.close();access=null;self.postMessage({ok:true});}catch(e){self.postMessage({error:e.message});}finally{if(access)access.close();}};
